query CountryQuery($code: ID!) {
    country(code: $code) {
        code
        continent {
            code
            name
        }
        currency
        emoji
        name
        native
        phone
        states {
            code
            name
        }
        languages {
            code
            name
            native
            rtl
        }
    }
}
